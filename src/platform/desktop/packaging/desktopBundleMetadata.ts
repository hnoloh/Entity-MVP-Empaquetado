export interface DesktopBundleMetadata {
  bundleIdentifier: string;
  productName: string;
  version: string;
  shortDescription: string;
  longDescription: string;
  category: string;
  publisher: string;
  license: string;
}
